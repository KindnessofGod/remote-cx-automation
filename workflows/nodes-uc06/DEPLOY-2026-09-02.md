# DEPLOY 2026-09-02 — UC-06 after the three-persona UI E2E

**Workflow `WORKFLOW_UC06_ID` — "RCX UC-06 🟡 Contract Amendment / Payroll Cutoff".**
Two things published, both proven from the read-back and then by driving the
production webhook, never by a deploy's exit code.

## What was published

| Node | Kind | Change | How |
|---|---|---|---|
| `Amendment Gates` | Code node (`amendmentGates.js`) | The ticket note names a PROJECTED cycle as projected (contract §8 inv. 13 — the expert review found rows carrying `cutoffCycle: "standin-nl-2026-10"` described as if Remote had published it); the deciding-gate sentence names slot 1 as *the employer's signatory* rather than *the customer admin* | `node scripts/deploy-node.mjs --publish …` — draft and production `versionId` equal after each publish |
| `Append Audit Log` | Supabase node, `details` expression (a node PARAMETER no file specified until today) | Adds `cutoffCycleProjected`, `cutoffCycleStandin`, `source` and `summary` — the four keys `src/uc06/workflow.js` has written since 2026-08-18 and the n8n path never did | `workflows/nodes-uc06/appendAuditLogSpec.js` is now the one authority; `scripts/deploy-uc06-audit-details.mjs` publishes it and refuses to report success on anything but a byte-identical read-back with `versionId === activeVersionId`. Before: **5 issues**; after: **0** |

`npm run verify-deployed`: **86 nodes checked · 0 drifted · 0 unpublished.**

## Proof — production webhook, `pinData: null`

| Execution | Input | Result |
|---|---|---|
| **12064** | The expert's b1 payload (NL `global_payroll_employee` `75b88008…`, salary 72,000 → 80,000 EUR, effective 2026-10-15) under ref `e2e-uc06-proof-2` | 13 nodes `success` through `Route by Decision`; `dual_approval_required / all_gates_passed`. **Audit row `ddab7859`** carries `cutoffCycle: "standin-nl-2026-10"`, **`cutoffCycleProjected: true`**, `cutoffCycleStandin: {projected: true, cadence: "period_start +19d = cutoff …", derivedFrom: "acb33b37…"}`, `source: "e2e-expert"`, `summary`. The gates node's note reads *"Payroll cycle: standin-nl-2026-10, locks 2026-10-20 (1135h from the decision). PROJECTED cycle — a stand-in continuation of the observed cadence, not one Remote published; confirm the lock against the real calendar before relying on it."* |

The run is marked `error` at the terminal Zendesk node only — a descriptive
`externalRef` where Zendesk wants a numeric ticket id, downstream of every
durable write. Read node status, never run status (CLAUDE.md §4).

**12062** is a failed attempt from the same minute and is not evidence of
anything: the request was sent with the saved capture's WRAPPER (`{ack, body,
sentAt, webhookStatus}`) instead of its `body`, and `Normalize Amendment
Request` refused it. Recorded so nobody reads it as a regression.

Earlier the same day the expert review proved **exactly-once on UC-06 for the
first time** (contract §16 item 9): execution **12038** wrote one claim, one
`uc06_amendments` row, one `audit_log` row and four traces; the redelivery
**12040** under the same ref stopped at `Duplicate Delivery — Stop` with
`main[0]` empty and `main[1]` populated, writing nothing. Seven live cases
(12038–12052) decided identically to `src/uc06` to the last decimal of the
hours. Evidence: `qa/evidence/UC-06/2026-09-02-uc06-e2e/expert/`.

## Still open on this graph

- **CA is semi-monthly live; the stand-in bridge projects only the 15th–month-end
  stream**, so a CA effective date in the first half of a month answers
  `no_matching_payroll_cycle` (execution 12052) and the note presents that as a
  fact about the calendar. A projection change in `src/remotebridge/`, its own
  work order.
- The terminal Zendesk nodes are unchanged; their prose spec
  (`terminalZendeskNodesSpec.js`) already forbids naming slot 1 "Customer Admin".
