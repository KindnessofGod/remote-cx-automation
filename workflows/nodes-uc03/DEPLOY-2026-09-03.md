# DEPLOY 2026-09-03 — UC-03 gains an engagement-eligibility gate

**Workflow `WORKFLOW_UC03_ID` — "RCX UC-03 🟢 Travel Support Letter / Workation Router".**
One Code node published, proven by driving the production webhook in both
directions, never by the deploy's exit code.

## What was published

| Node | Change | How |
|---|---|---|
| `Travel Router Gates` | Ports the engagement-eligibility gate added to `src/uc03/policyEngine.js`: a non-EOR engagement is refused before identity is even checked. The five `means` strings are copied byte-for-byte from the reference ladder, which `test/n8nUc03Parity.test.js` and `test/n8nUc03TerminalZendeskNodes.test.js` both enforce | `node scripts/deploy-node.mjs --publish …` — draft and production `versionId` equal after publishing (`ac659c0f`) |

`npm run verify-deployed`: **86 nodes checked · 0 drifted.**

## Proof — production webhook, `pinData: None`

| Execution | Input | Result |
|---|---|---|
| **12333** | Alexandre Tremblay, `contract_type: contractor`, asking for a visa support letter | `blocked / engagement_not_eor_contractor`, `flags: ["engagement_contractor"]` |
| **12334** | Alex Morgan, an EOR employee, the **same words** | Passes the engagement gate and decides `human_review / formal_letter_requested` |

12334 is the negative control and it is the load-bearing half: it shows the gate
DISCRIMINATES on the engagement rather than refusing every travel letter.

**Every durable write landed on the blocked run before it stopped.** Execution
12333's node trail is green through `Claim Ticket (Idempotency)`, `Append Audit
Log`, `Collect Trace Steps`, `Append Audit Trace`, `Carry Context Forward`,
`Assign Routing` and `Route by Decision`. Read node status, never run status.

## STILL OPEN — the graph has no terminal branch for `blocked`

`Route by Decision` has outputs for `auto_resolve`, `human_review`, `escalate`
and `route_to_uc04`. `blocked` is new, so execution 12333 left by the catch-all
`Unrecognised Travel Decision`, which then failed on a descriptive
`externalRef`. The decision is correct, recorded and audited; what is missing is
the node that would tell the requester on a Zendesk-originated ticket.

**Not done here on purpose.** Adding a Switch output and wiring a node is a
production graph-SHAPE change, and CLAUDE.md §7b's standing authorisation covers
publishing a tested node body and proving it, not re-wiring a live graph. It
needs the owner's explicit go-ahead, the way the UC-01 consent-lookup node did
(`qa/HUMAN-DECISIONS-REQUIRED.md` §K4).

**The portal path is unaffected and complete**: a portal-filed request is
refused on the page with Remote's own reason and the remedy, and raises no
ticket at all (`NO_TICKET_DECISIONS.uc03` now carries `blocked`), because a fact
Remote publishes is not something a specialist can overturn.
